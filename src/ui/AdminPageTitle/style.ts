import styled from 'styled-components';

export const StyledAdminPageTitle = styled.h1`
  font-family: var(--primary-font);
  font-size: 1.666666rem;
  font-style: normal;
  font-weight: 500;
  line-height: 1.1111rem;
  margin-bottom: 25px;
  padding-top: 35px;
  color: var(--blue-700);
`;
