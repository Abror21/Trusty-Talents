import styled from 'styled-components';

export const StyledAddUserActionForm = styled.div`
  .title {
    font-family: var(--primary-font);
    font-size: 1.66667rem;
    line-height: normal;
    font-weight: 700;
    padding: 20px 0;
  }

  .input-box {
    display: flex;
    align-items: center;
    justify-content: space-between;
  }
`;
